apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - alertmanager-deployment.yaml
  - alertmanager-service.yaml

# Optional Alertmanager Secret generator:
# Copy alertmanager-secret.example.yaml to alertmanager-secret.yaml, replace WRITE_YOUR_PASSWORD_HERE,
# then uncomment this block only in a local/private overlay.
# This creates secret/alertmanager-config-secret with key alertmanager.yml.
# secretGenerator:
#   - name: alertmanager-config-secret
#     namespace: prometheus
#     files:
#       - alertmanager.yml=alertmanager-secret.yaml
#
# generatorOptions:
#   disableNameSuffixHash: true
